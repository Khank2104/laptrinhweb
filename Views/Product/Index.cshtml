@model IEnumerable<_2280601466_NguyenNgocKhanh.Models.Product>

<h2>Product Management</h2>
<a asp-controller="Home" asp-action="Index" class="btn btn-secondary mb-3">Quay lại</a>
<!-- Phần còn lại của trang -->

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Danh sách sản phẩm</h2>
        <a asp-action="Add" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Thêm sản phẩm
        </a>
    </div>

    <div class="card shadow">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Tên</th>
                        <th>Giá</th>
                        <th>Mô tả</th>
                        <th class="text-center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <td>@product.Name</td>
                            <td>@String.Format("{0:N0} ₫", product.Price)</td>
                            <td>@product.Description</td>
                            <td class="text-center">
                                <a asp-action="Display" asp-route-id="@product.Id" class="btn btn-info btn-sm me-1">
                                    <i class="bi bi-eye"></i> Xem
                                </a>
                                <a asp-action="Update" asp-route-id="@product.Id" class="btn btn-warning btn-sm me-1">
                                    <i class="bi bi-pencil-square"></i> Sửa
                                </a>
                                <a asp-action="Delete" asp-route-id="@product.Id" class="btn btn-danger btn-sm">
                                    <i class="bi bi-trash"></i> Xóa
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>